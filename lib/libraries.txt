# For LLM

The LLM should be installed in this directory so the path can be accepted by the entrypoint

# List of Libraries

customtkinter
torch
transformers
huggingface
numpy
safetensors
tokenizers
sympy
uvicorn
